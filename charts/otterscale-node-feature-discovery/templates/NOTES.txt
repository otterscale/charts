OtterScale Node Feature Discovery has been installed.

  Namespace: {{ .Release.Namespace }}

  Components:
  - node-feature-discovery (subchart): nfd-master and nfd-worker
  {{- if .Values.featureWriter.enabled }}
  - feature-writer DaemonSet: refreshes
      /etc/kubernetes/node-feature-discovery/features.d/{{ .Values.featureWriter.featureFileName }}
    every {{ .Values.featureWriter.intervalSeconds }}s for the NFD local source.
  {{- end }}

  Label namespace allowlist (master): otterscale.io (values: node-feature-discovery.master.extraLabelNs)

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
  - kubectl get nodes --show-labels | grep otterscale
