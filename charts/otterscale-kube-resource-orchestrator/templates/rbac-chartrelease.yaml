apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
  name: kro-chartrelease-admin-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - chartreleases
    verbs:
      - "*"
  - apiGroups:
      - kro.run
    resources:
      - chartreleases/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
  name: kro-chartrelease-editor-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - chartreleases
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  - apiGroups:
      - kro.run
    resources:
      - chartreleases/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    rbac.authorization.k8s.io/aggregate-to-view: "true"
  name: kro-chartrelease-viewer-role
rules:
  - apiGroups:
      - kro.run
    resources:
      - chartreleases
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - kro.run
    resources:
      - chartreleases/status
    verbs:
      - get
