# Values passed through to the upstream kro chart.
kro:
  config:
    featureGates:
      # Required by the helmrelease RGD, which uses the omit() CEL function
      # to drop unset optional HelmRelease fields from the rendered resource.
      CELOmitFunction: true
