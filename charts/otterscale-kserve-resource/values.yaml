kserve-llmisvc-resources:
  kserve:
    controller:
      llmisvc:
        enabled: true
    localmodel:
      enabled: true

envoyProxy:
  # LoadBalancer / ClusterIP / NodePort
  type: NodePort

prometheus:
  serviceMonitor:
    enabled: true
