kserve-llmisvc-resources:
  kserve:
    controller:
      llmisvc:
        enabled: true

envoyProxy:
  # LoadBalancer / ClusterIP / NodePort
  type: NodePort

gateway:
  name: kserve-ingress-gateway

prometheus:
  serviceMonitor:
    enabled: true
