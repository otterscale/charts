OtterScale llm-d Infrastructure has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-gpu-operator must be installed first

  Components:
  - Gateway API configuration for LLM inference

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }}
