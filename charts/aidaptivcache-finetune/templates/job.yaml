apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "aidaptivcache-finetune.fullname" . }}
  labels:
    {{- include "aidaptivcache-finetune.labels" . | nindent 4 }}
spec:
  backoffLimit: {{ .Values.job.backoffLimit }}
  {{- if .Values.job.ttlSecondsAfterFinished }}
  ttlSecondsAfterFinished: {{ .Values.job.ttlSecondsAfterFinished }}
  {{- end }}
  template:
    metadata:
      labels:
        {{- include "aidaptivcache-finetune.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: {{ .Values.job.restartPolicy }}
      {{- if .Values.schedulerName }}
      schedulerName: {{ .Values.schedulerName }}
      {{- end }}
      initContainers:
      {{- if .Values.initContainer.enabled }}
      - name: model-loader
        image: "{{ .Values.initContainer.image.repository }}:{{ .Values.initContainer.image.tag }}"
        imagePullPolicy: {{ .Values.initContainer.image.pullPolicy }}
        command:
        - /bin/sh
        - -c
        - |
          cp -a {{ .Values.initContainer.modelSourcePath }}/ {{ .Values.initContainer.sharedDataPath }}/
          echo "Model copied from {{ .Values.initContainer.modelSourcePath }} to {{ .Values.initContainer.sharedDataPath }}"
          {{- if .Values.prescript }}
{{ .Values.prescript | indent 10 }}
          {{- end }}
        volumeMounts:
        - name: shared-data
          mountPath: {{ .Values.initContainer.sharedDataPath }}
      {{- end }}
      {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "crane") }}
      - name: crane-setup
        image: {{ include "aidaptivcache-finetune.craneImage" . | quote }}
        imagePullPolicy: IfNotPresent
        command: ["cp", "/ko-app/crane", "/crane-bin/crane"]
        volumeMounts:
        - name: crane-bin
          mountPath: /crane-bin
      {{- end }}
      {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "kitops") }}
      - name: kitops-setup
        image: {{ include "aidaptivcache-finetune.kitopsImage" . | quote }}
        imagePullPolicy: IfNotPresent
        command:
        - /bin/sh
        - -c
        - |
          cp -a {{ .Values.outputImage.kitops.binPath | default "/usr/local/bin/kit" | quote }} /kitops-bin/kit
          chmod +x /kitops-bin/kit
        volumeMounts:
        - name: kitops-bin
          mountPath: /kitops-bin
      {{- end }}
      containers:
      - name: finetune
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        command:
        - /bin/sh
        - -c
        - |
          mkdir -p {{ .Values.envConfig.pathSettings.outputDir }}
          echo "Starting training job..."
          phisonai2 --env_config /config/env/env_config.yaml --exp_config /config/exp/exp_config.yaml
          TRAIN_EXIT_CODE=$?
          echo "Training finished with exit code: $TRAIN_EXIT_CODE"
          {{- if .Values.outputImage.enabled }}
          if [ $TRAIN_EXIT_CODE -eq 0 ]; then
            {{- if eq .Values.outputImage.type "crane" }}
            echo "Packaging output model as OCI image (crane)..."
            tar -cf {{ .Values.envConfig.pathSettings.outputDir }}/model-layer.tar -C {{ .Values.envConfig.pathSettings.outputDir }} .
            {{ include "aidaptivcache-finetune.craneBin" . }} append \
              --base {{ include "aidaptivcache-finetune.craneBaseImage" . }} \
              -f {{ .Values.envConfig.pathSettings.outputDir }}/model-layer.tar \
              {{- if .Values.outputImage.insecure }}
              --insecure \
              {{- end }}
              -t {{ .Values.outputImage.repository }}:{{ .Values.outputImage.tag }}
            PUSH_EXIT_CODE=$?
            rm -f {{ .Values.envConfig.pathSettings.outputDir }}/model-layer.tar
            {{- else if eq .Values.outputImage.type "kitops" }}
            echo "Packaging output model as ModelKit (kitops)..."
            {{ include "aidaptivcache-finetune.kitopsBin" . }} init {{ .Values.envConfig.pathSettings.outputDir }}
            {{ include "aidaptivcache-finetune.kitopsBin" . }} pack {{ .Values.envConfig.pathSettings.outputDir }} \
              --use-model-pack \
              -t {{ .Values.outputImage.repository }}:{{ .Values.outputImage.tag }}
            PACK_EXIT_CODE=$?
            if [ $PACK_EXIT_CODE -ne 0 ]; then
              echo "Failed to pack model (exit code: $PACK_EXIT_CODE)"
              exit $PACK_EXIT_CODE
            fi
            {{ include "aidaptivcache-finetune.kitopsBin" . }} push {{ .Values.outputImage.repository }}:{{ .Values.outputImage.tag }}{{ if .Values.outputImage.insecure }} --plain-http{{ end }}
            PUSH_EXIT_CODE=$?
            {{- end }}
            if [ $PUSH_EXIT_CODE -eq 0 ]; then
              echo "Model pushed to {{ .Values.outputImage.repository }}:{{ .Values.outputImage.tag }}"
            else
              echo "Failed to push model (exit code: $PUSH_EXIT_CODE)"
              exit $PUSH_EXIT_CODE
            fi
          fi
          {{- end }}
          exit $TRAIN_EXIT_CODE
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
        volumeMounts:
        {{- if .Values.modelImage.enabled }}
        - name: model-volume
          mountPath: {{ .Values.modelImage.mountPath }}
          readOnly: true
        {{- end }}
        {{- if .Values.initContainer.enabled }}
        - name: shared-data
          mountPath: {{ .Values.initContainer.sharedDataPath }}
        {{- end }}
        {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "crane") }}
        - name: crane-bin
          mountPath: /crane-bin
          readOnly: true
        {{- end }}
        {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "kitops") }}
        - name: kitops-bin
          mountPath: /kitops-bin
          readOnly: true
        {{- end }}
        {{- if .Values.outputImage.pushSecret }}
        - name: docker-config
          mountPath: /root/.docker
          readOnly: true
        {{- end }}
        - name: env-config
          mountPath: /config/env
          readOnly: true
        - name: exp-config
          mountPath: /config/exp
          readOnly: true
        - name: train-data-config
          mountPath: /config/train_data
          readOnly: true
      volumes:
      {{- if .Values.modelImage.enabled }}
      - name: model-volume
        image:
          reference: {{ .Values.modelImage.reference | quote }}
          pullPolicy: {{ .Values.modelImage.pullPolicy }}
      {{- end }}
      {{- if .Values.initContainer.enabled }}
      - name: shared-data
        emptyDir: {}
      {{- end }}
      {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "crane") }}
      - name: crane-bin
        emptyDir: {}
      {{- end }}
      {{- if and .Values.outputImage.enabled (eq .Values.outputImage.type "kitops") }}
      - name: kitops-bin
        emptyDir: {}
      {{- end }}
      {{- if .Values.outputImage.pushSecret }}
      - name: docker-config
        secret:
          secretName: {{ .Values.outputImage.pushSecret }}
          items:
          - key: .dockerconfigjson
            path: config.json
      {{- end }}
      - name: env-config
        configMap:
          name: {{ include "aidaptivcache-finetune.fullname" . }}-env-config
      - name: exp-config
        configMap:
          name: {{ include "aidaptivcache-finetune.fullname" . }}-exp-config
      - name: train-data-config
        configMap:
          name: {{ include "aidaptivcache-finetune.fullname" . }}-train-data
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
