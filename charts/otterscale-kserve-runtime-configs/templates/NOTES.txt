OtterScale KServe (LLMInferenceService) has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-envoy-gateway must be installed first
  - otterscale-envoy-ai-gateway must be installed first
  - otterscale-leader-worker-set must be installed first

  Components:
  - KServe LLMInferenceService CRDs
  - KServe controller (LLMIsvc mode)

  Next steps:
  - Create an LLMInferenceService resource to deploy a model

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }}
  - kubectl get crds | grep llminferenceservices
