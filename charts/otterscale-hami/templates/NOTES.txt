OtterScale HAMI has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-gpu-operator must be installed first

  Components:
  - HAMI Scheduler
  - HAMI Device Plugin (GPU sharing with device split)

  Configuration:
  - Device split count: {{ .Values.hami.devicePlugin.deviceSplitCount }}
  - Runtime class: {{ .Values.hami.devicePlugin.runtimeClassName }}

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }} -l app=hami
