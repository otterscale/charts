OtterScale Envoy AI Gateway has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-envoy-gateway must be installed first
  - otterscale-envoy-ai-gateway-base must be installed first

  Components:
  - Envoy AI Gateway controller

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }}
