OtterScale KubeVirt has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-cdi must be installed first

  Components:
  - KubeVirt CRDs
  - KubeVirt Operator
  - KubeVirt custom resource

  Note: Resources are managed as templates and will be removed on helm uninstall.

  Verify:
  - kubectl get pods -n {{ .Release.Namespace }}
  - kubectl get kubevirt
