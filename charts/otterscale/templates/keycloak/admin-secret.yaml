{{- if .Values.keycloakx.enabled -}}

{{- $secretName := printf "%s-admin" (include "otterscale.keycloakx.fullname" .) -}}
{{- $passwordKey := "KC_BOOTSTRAP_ADMIN_PASSWORD" -}}

apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "otterscale.namespace" . }}
  labels:
    app.kubernetes.io/component: keycloakx
    app.kubernetes.io/part-of: keycloakx
    {{- include "otterscale.labels" . | nindent 4 }}
type: Opaque
data:
  {{ $passwordKey }}: {{ include "otterscale.keycloakx.adminPassword" . | b64enc | quote }}
{{- end }}
