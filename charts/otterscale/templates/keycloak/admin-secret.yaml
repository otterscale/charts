{{- if .Values.keycloakx.enabled -}}

{{- $secretName := printf "%s-keycloakx-admin" .Release.Name -}}
{{- $passwordKey := "KC_BOOTSTRAP_ADMIN_PASSWORD" -}}

apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ include "otterscale.namespace" . }}
  labels:
    app.kubernetes.io/component: keycloakx
    app.kubernetes.io/part-of: keycloakx
    {{- include "otterscale.labels" . | nindent 4 }}
type: Opaque
data:
  {{ $passwordKey }}: {{ include "common.secrets.passwords.manage" (dict "secret" $secretName "key" $passwordKey "providedValues" (list "keycloakx.auth.adminPassword") "length" 10 "strong" false "failOnNew" false "context" $) }}
{{- end }}
