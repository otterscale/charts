apiVersion: v1
kind: Secret
metadata:
  name: {{ include "otterscale.fullname" . }}-postgres
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "otterscale.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
type: Opaque
data:
  postgres-db: {{ .Values.keycloakx.database.database | b64enc | quote }}
  postgres-user: {{ .Values.keycloakx.database.username | b64enc | quote }}
  postgres-password: {{ .Values.keycloakx.database.password | b64enc | quote }}