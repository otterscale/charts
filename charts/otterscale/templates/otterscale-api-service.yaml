apiVersion: v1
kind: Service
metadata:
  name: {{ include "otterscale.api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "otterscale.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: NodePort
  selector:
    {{- include "otterscale.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  ports:
    - name: connectrpc
      port: {{ .Values.otterscaleApi.ports.http }}
      targetPort: http
      nodePort: {{ .Values.otterscaleApi.nodePorts.http }}
      protocol: TCP
    - name: tunnel
      port: {{ .Values.otterscaleApi.ports.tunnel }}
      targetPort: tunnel
      nodePort: {{ .Values.otterscaleApi.nodePorts.tunnel }}
      protocol: TCP
