{{- if and .Values.keycloakx.enabled .Values.keycloakx.database.persistence.enabled -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "otterscale.fullname" . }}-postgres-data
  namespace: {{ .Release.Namespace }}
  annotations:
    # Prevent accidental data loss on `helm uninstall`.
    # The PVC must be deleted manually when the data is no longer needed.
    helm.sh/resource-policy: keep
  labels:
    {{- include "otterscale.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  accessModes:
    - {{ .Values.keycloakx.database.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.keycloakx.database.persistence.size }}
  {{- with include "otterscale.storageClassName" . }}
  storageClassName: {{ . }}
  {{- end }}
{{- end }}
