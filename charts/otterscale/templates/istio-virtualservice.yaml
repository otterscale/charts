{{- if and .Values.istio.enabled .Values.istio.virtualService.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "otterscale.fullname" . }}
  namespace: istio-system
  labels:
    {{- include "otterscale.labels" . | nindent 4 }}
spec:
  hosts:
    {{- toYaml .Values.istio.gateway.hosts | nindent 4 }}
  gateways:
    - {{ include "otterscale.gatewayRef" . }}
  http:
    # ---- Keycloak routes ----
    - match:
      - uri:
          prefix: /auth/
      route:
      - destination:
          host: {{ .Release.Name }}-keycloakx-http.{{ .Release.Namespace }}.svc.cluster.local
          port:
            number: 80
      headers:
        request:
          set:
            X-Forwarded-Proto: {{ if and .Values.istio.gateway .Values.istio.gateway.tls .Values.istio.gateway.tls.enabled }}"https"{{ else }}"http"{{ end }}
    # ---- OtterScale routes ----
    - match:
      - uri:
          prefix: /api/
      rewrite:
          uri: /
      route:
      - destination:
          host: {{ include "otterscale.api.fullname" $ }}.{{ .Release.Namespace }}.svc.cluster.local
          port:
            number: 8299
    - match:
      - uri:
          prefix: /vnc/
        headers:
          upgrade:
            exact: websocket
      route:
      - destination:
          host: {{ include "otterscale.api.fullname" $ }}.{{ .Release.Namespace }}.svc.cluster.local
          port:
            number: 8299
    - match:
      - uri:
          prefix: /
      route:
      - destination:
          host: {{ include "otterscale.dashboard.fullname" $ }}.{{ .Release.Namespace }}.svc.cluster.local
          port:
            number: 3000
{{- end }}