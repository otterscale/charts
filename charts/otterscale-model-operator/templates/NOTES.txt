Model Operator has been installed successfully!

Release name: {{ .Release.Name }}
Namespace: {{ .Values.namespace.name }}

The Model Operator manages AI/ML model resources on Kubernetes.

To verify the installation:

  kubectl get deployment -n {{ .Values.namespace.name }} model-operator-controller-manager

To view the operator logs:

  kubectl logs -n {{ .Values.namespace.name }} -l control-plane=controller-manager -f

For more information, visit:
  https://github.com/otterscale/model-operator
