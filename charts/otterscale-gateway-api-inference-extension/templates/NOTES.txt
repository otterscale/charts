OtterScale Gateway API Inference Extension has been installed.

  Namespace: {{ .Release.Namespace }}

  Prerequisites:
  - otterscale-gateway-api must be installed first

  Components:
  - Inference Extension CRDs
  - Inference Extension controllers

  Note: Resources are managed as templates and will be removed on helm uninstall.

  Verify:
  - kubectl get crds | grep inference
